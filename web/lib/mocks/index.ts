/**
 * Mock data barrel. API-CLIENT owns lib/mocks/*.
 *
 * Re-exports the mock fixtures. Domain TYPES (incl. TargetLibraryEntry) live in
 * lib/types/domain.ts; fixtures import them from there.
 */

export * from "./fixtures";
